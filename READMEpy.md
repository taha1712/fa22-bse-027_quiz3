# Python Branch

This branch contains:
- A simple Python program (`hello.py`) that prints "Hello World".
- A Jenkins pipeline (`Jenkinsfile`) to run the Python script.
- This branch is used to demonstrate Python + Jenkins integration.
